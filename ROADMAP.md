# 待办

这份清单以前只活在聊天记录里。写下来是因为有一次问「还剩什么」，
翻遍仓库找不到第二个记着这些的地方。

排序按「不做会怎样」，不按「做起来多有意思」。
做完一件就从这儿删掉，别留一堆划掉的行 —— 那样过三个月就没人看了。

---

## 一、已经欠着的

### 隐私政策 + 服务条款（已有初版，还欠一件）

两份文档在 `src/lib/legal.ts`，App 里「我的 → 隐私政策与服务条款」
看得到，注册那一屏也链过去了。

还欠的：

- **不是律师写的。** 是照着「这个 App 实际在做什么」一条条列的。
  PDPA 的具体措辞要不要找人看一眼，值得花那笔钱的时候再说。

改了功能就回来改这两份。`legal.ts` 开头写了这条规矩。

### ~~一键删号~~（2026-09-16 做了，并且真删了一个账号验过）

「我的 → 注销账号」。上架应用商店的硬性要求（Apple 5.1.1(v)、
Google Play），也是政策里承诺过的事 —— 原来写的是「写信到那个邮箱」。

**线上真删过一次**（测试账号 Elviss）：账号、球群成员资格、全国榜那一行
都回到了删之前的基线；而那一行球员**还在**、名字还是 Elviss、`ownerId`
是 null、`deleted` 是 false。四条同时成立，「比分要留、人要脱钩」才算验过。

**那次真删抓到两个只有上线才撞得到的东西**：

- **`service_role` 在 `records` 上只有 `select`。** 001 里那一句是为了让推送
  函数读得到名字，没人想到以后会有函数要写。删号炸在
  `permission denied for table records`，020 补了 `grant update`。
- **函数自己要回 CORS 预检。** `notify-*` 没写 CORS 也能用，是因为它们的
  「Verify JWT with legacy secret」开着、网关替它们回了；`delete-me` 自己
  验身份、那个开关按 Supabase 的建议关掉了，于是预检和错误响应都落到
  函数头上。不带 CORS 头的话，界面只会显示一句
  「Failed to send a request」，真正的错误读都读不到。

顺带钉住的一条：`unlinkPlayers` 那个 UPDATE 会数受影响行数，0 行就抛。
本机跑真 Postgres 撞出来的 —— `service_role` 少了 BYPASSRLS 的话那句
**不报错、只动 0 行**（实测 0 vs 1），函数会一路返回成功，而账号删了、
`ownerId` 还连着，没人会发现。

做法：绝大部分连带删除交给**外键**（`supabase/020-delete-account.sql`
把 `club_members` 和 `push_subscribers` 那两处漏掉的补上了），
Edge Function 只做外键做不到的三件事 —— 剪断球员行的 `ownerId`、
删 Storage 里的语音文件、按本人选择抹不抹名字。

交给外键而不是写进函数，是因为**谁保证**不一样：写在函数里，
从后台手动删一个用户就全绕过去了。

**已知的洞，写在这儿免得三个月后被当成新发现**：针对一个人的举报是
`on delete cascade` 的，所以注销会连带删掉别人对他的举报 ——
**重新注册可以洗掉历史**。选择这样是因为举报里冻着他的私聊作为证据，
留着它和「删号」是矛盾的。真要堵这个洞得先有封号和仲裁，而那两件
本来就还没做。

### ~~备份恢复演练~~（2026-09-16 跑过了，结论：救得回来）

整套走完了：导出 → 恢复到一个临时项目 → 12 项数字逐个对 → 删掉临时项目。
**一行没丢**，账号和比分也是接上的（有主人的球员 17 == 17）。

`docs/备份与恢复.md` 按实跑重写过了，最后那张演练记录表里填了第一行，
并且记着抓到的七件事。其中最要紧的一件不在备份上：

> **dump 文件里有一把 service_role 钥匙** —— 开局那个数据库 Webhook
> 把它写死在触发器定义里，于是每一份备份都是一把能打开整个数据库的
> 主钥匙。那个触发器本身还是多余的（手机那条路一直在干活），已经删掉，
> 见 `supabase/019-drop-session-webhook.sql`。

**下一次该做的**：语音文件那一段（Storage 的 `voice` 桶）还没实跑过，
手册里写着但没验。以及按手册说的，一个月跑一次。

### 错误监控（做了，但收不到登录之前的）

App 撞上没接住的错误会自己报上来，管理员在「我的 → 反馈与报错」
的第二个 tab 里看，按 bug 归并（撞了几次、哪些机型、哪一版）。

**收不到的那部分**：登录之前的崩溃。没登录时没有身份，要收就得给
`anon` 开一个谁都能写的口子，那是往免费版数据库上开一个没有门的洞。
而「第一次打开就白屏」恰恰是这一类 —— 这个缺口是真的，
不是忘了做。哪天真需要，得先想清楚怎么防刷。

### ~~推送那两个 Edge Function 不验调用者是谁~~（2026-09-19 补上了）

原来一个字都不问，挡在前面的只有 Supabase 网关那个「Verify JWT」开关 ——
而它认的是「有没有一把这个项目的钥匙」，不是「你是谁」。anon key
打包在前端里，谁都看得到。

**动手时发现 notify-session 比原来记的严重得多。** 这份清单上原来写的是
「最坏是把一条真实存在的消息的提醒重推一遍，内容捏造不了」——
那句话只对 notify-social 成立。notify-session **整条通知的文字都来自
请求体**（球馆名、上限人数、开局的人），而且它推给订阅表里的每一个人：
拿到那个地址的人可以让所有装了这个 App 的手机弹出他想写的任何一句话。

补法不是只验登录，是**验到「这件事跟你有关」**：

| 哪一种 | 要求调用者是 |
|---|---|
| 私信提醒 | 这条消息的发件人 |
| 好友申请 / 同意 | 按下那一步的那个人（申请是申请人，同意是同意的人）|
| 举报 | 举报的那个人（这一支还会拍聊天快照，不验能被反复触发）|
| 反馈 | 写反馈的那个人 |
| 开局广播 | `createdBy` 那个球员的主人 |

那几行本来就要从数据库读出来（通知内容只信数据库、不信请求体），
所以这一步没多花一次查询。

**代价写在明面上**：一个登录了、但球员身份还没认领（`ownerId` 空着）
的人开局，广播发不出去了。少一条没名字的通知，比留着一个任何人都能
广播任意文字的口子划算 —— 日志里会写清楚是被哪一条挡的。

两个函数同时补上了自己回 CORS 预检 —— `delete-me` 那次就是开关一关、
预检没人回，界面上只显示一句「Failed to send a request」，而函数一次
都没被调到。

**网关那个「Verify JWT」开关保持开着，而且不打算关了。**
这条待办原来写的是「补完再把它关掉」，那是在它还是唯一一道门、
语义又不清楚的前提下说的。现在函数自己有门了，网关那道就是白送的
第二道：它拦掉的是连项目钥匙都没有的请求，压根到不了函数这边。
关掉只少一道门，换不来任何东西。CORS 那一段照样留着 —— 哪天真要关
（比如换网关配置），不用再补一次。

顺带把这两个函数从「一条测试都没有」变成有 20 条
（`tests/notify-auth.test.ts`）：它跑的是真要部署上去的那份源码，
只把 `npm:` 那两个依赖换成假的。这是整个项目里唯一一处没法在本机跑真
Postgres 撞出来的权限 —— 它长在 Deno 那一侧，不在数据库策略里。

### 开局广播不分球群

`notify-session` 推给 `push_subscribers` 里的**每一个人**，不看球群。
所以「我的球群」开一场局，Twin ark 的人也会收到通知。

改起来不难（按开局那个球员的 `club_id` 把订阅筛一遍），但这是**产品
决定不是 bug 修复**：ROADMAP 上「附近球局 + 横向找局」那一条想要的
恰恰是跨群看得到局。两件事得一起想，所以先记在这儿。

---

## 二、用起来最缺的

### 对手确认记分（2026-09-19 补上了仲裁）

一场打完，场上其他装了 App 的人会被问一句「这个比分对吗」，
在看板和结算两处都能点。说「不对」的人要报出自己认为的比分，
拿手机的人一个按钮就能采纳。规矩在 `src/lib/confirm.ts`。

**两边都不肯让的那条死路，现在有出口了：管理员把这一场作废。**

他判的**不是谁对**（不在场、没得看，判了也是猜），是「这一场不作数」。
作废之后四个人的 MMR、段位、金币、胜率当场跟着变 —— 挡在
`decidedMatches` 那一处，所以每条统计路径同时生效，一处都不会漏。

三条硬规矩，都在 `tests/confirm.test.ts` 里钉着：

1. **只有被提过异议的场次作废得了。** 没有这条，管理员手上就是一个
   「随时抹掉任何一场」的按钮 —— 那不是仲裁，是改历史。
2. **异议本身永远不等于作废。** 那是给输的人一个稳赚的按钮，
   每个输的人都会点。中间必须隔着一个管理员。
3. **作废可逆、看得见、会自己撤。** 那一场留在历史里划着线、写着为什么；
   争议一旦谈拢（采纳了对方报的数，或者退回去重记），作废自动撤掉 ——
   走的是和「改了比分就作废旧确认」同一份 `CLEAR_CONFIRMATIONS`。

**没做进数据库的那一半，说清楚**：作废只在界面上限管理员。
`records` 的写策略对同群的人是全开的（见 `supabase/006-who-can-delete.sql`
那句「不是在删东西：随便改」），也就是说同群的人本来就能改彼此的比分 ——
作废不比改比分更危险，单给它加一道数据库的门只会是自欺。
真要收紧的话，要收紧的是整张 `records` 的写入，不是这一个字段。

### ~~封号 / 禁言~~（2026-09-18 做了，025）

做举报时留的口子，现在补上了。**申诉和它同时上线**，因为这一条一直
写在这儿：一个没有申诉的封号按钮，是另一种伤害，不是解决方案。

两档，而且**都不碰打球**：被封的人照样记分、开球局、看排行榜 ——
球馆里常常是一台手机轮流记分，为一件社交上的事让整场球记不了分，
伤的是另外七个没做错事的人。

  禁言  发不了私信、发不了动态、点不了赞
  封号  再加：加不了好友、上不了全国榜、报不了「正在打」、改不了名片

挡人的是**触发器**不是策略：RLS 拒绝只会说一句
「violates row-level security policy」，而一个被封的人最该知道的
就是「为什么、到什么时候」。

申诉那张表上**一个挡发言的触发器都没有**，这是故意的，也钉在自检里。
管理员点「通过」的那一下数据库自己解封（触发器），不用他再点一次 ——
少那一步是因为忘了的话，人会看到「申诉通过」却还是发不出话。

还欠一件：**举报和封号还是全 App 的，不按球群分**（见下一条）。

### ~~球群里的球员删不掉~~（2026-09-19 做了）

`setPlayerArchived` 在 store 里放了很久，**一处调用都没有** —— 界面上
从来没做出那个按钮。发现它是因为删号演练：测试账号注销之后球群里留下
一个没有主人、0 场比赛的球员，管理员没有任何办法清掉（最后是手写 SQL
软删的）。后来 Py 那次又来了一遍。

现在「我的 → 球群成员」那一屏能收能放：

- **收起来不是删掉。** 他打过的每一场原样留着 —— 那些同时也是另外三个
  人的战绩，删掉等于改了别人的历史。和注销账号那边同一条规矩：
  **人要脱钩，比分要留**。
- **收起来的那些也列在同一屏上。** 一个收得起来但找不回来的按钮，
  比没有那个按钮更吓人。
- **两种情形挡住**（`src/lib/roster.ts`，10 条单测）：收自己（你会从
  自己球群的每个名单里消失，而账号和比分都还在 —— 要离开有别的路），
  以及收一个还在进行中的球局里的人（球局记的是 playerIds、不看这个
  标记，收了之后场上会挂着一个名单里查不到的人）。

别的地方本来就认这个标记（选人 `AddGuest`、本群排名 `GlobalRanking`、
好友搜索、`feed`），所以这次只补了按钮，没动过滤。

球馆榜那种按场次算的地方**故意不过滤**：那张榜回答的是「在这个球馆
打过的人里谁强」，一个已经不打了的人当年确实打过。

### 管理员是全 App 的，不是按球群分的

加进 `app_admins` 的人看得到**所有球群**的举报，包括里面附的私聊。
只有一个球群的时候这没区别；真开放出去之后，「帮我管管周三那场」
的人不该顺带看得到别的球群的私聊。

改法是给举报加上球群，管理员按群授权。不难，但要等真有第二个群
在用才知道该怎么分 —— 现在做只能靠猜。

在那之前，加管理员那一屏上写明了他会看到什么。

---

## 三、想做的

### 地址搜索找不到马来西亚的小球馆

这不是排序问题，是数据源问题：那个免费服务（photon）背后是
OpenStreetMap，而马来西亚大多数羽球馆**根本不在 OSM 里**。
实测搜「Long river sport」回来的是砂拉越的六条河 —— 而那个球馆
在 Google 地图上有一百多条评价。

已经做的三层（马来西亚 bbox、加重距离偏好、按国家排）把「跳去英国」
治住了，但变不出没有的东西。所以现在的路是：**手打地址 + 从
Google 地图复制坐标粘进来**，两个入口都在那一屏上写明了。

真要根治只有两条：花钱上 Google Places API（要密钥、要计费），
或者自己攒一份本地球馆库（球友填的地址本来就在往那个方向攒）。
等球群多起来再看哪条划算。

### 附近球局 + 横向找局

看得到附近有谁开了局，一排卡片向右划着找。

**前置**：球馆要有坐标。最省事的办法是你打球时顺手把常去那几个
在 App 里定个位，攒够了再开工 —— 没有坐标，这个功能连第一屏都画不出来。

### 地区 / 国家排名（做了，两个已知缺口）

「排名」那一屏多了「全国」一栏。上榜是自愿的（默认不在），
榜上显示名字、MMR、胜负、州，以及**有多少场被对手确认过**——
最后那个数是唯一不容易造假的，所以它和 MMR 并排摆着。

做法见 `supabase/017-leaderboard.sql` 开头：各自的手机报各自的结果，
比赛记录一行都不出自己的球群。

**缺口一：数字是自己报的。** 手改一个 MMR 传上去，数据库拦不住 ——
它只拦「不许写别人那一行」。真要拦得住，得服务端自己重算一遍 MMR，
那就有了两份会各自演化的实现。现在的办法是把底细摊开（那个 ✓ 数），
不是假装它权威。

**缺口二：要手动点「更新我的成绩」。** 打完新的之后榜上还是旧数，
那一屏会提示对不上。做成自动推的话，每次同步都要多一次写 ——
等真有人在用了再看值不值。

**串场的人现在算一个人了**（`supabase/018-cross-club.sql`，**要跑一次**）。
一个人在几个球群打，就一个群报一行，榜上按人合并。原来 uid 是主键，
于是在第二个群按一次「更新我的成绩」，第一个群那一份**悄悄被盖掉** ——
没有报错，几十场就没了。

那几行不存 club_id，存的是 uid 和群 id 一起算的散列：存 club_id 的话，
这张谁都读得到的表就顺手泄了一件本来读不到的事 —— 谁和谁一个群。

**合并里唯一不精确的地方**：MMR 是各群分别算完再相加，和把所有比赛
混在一起重放一遍不完全等价（「输到 0 就不再往下扣」在每个群里各生效
一次，所以相加会略高一点）。差别只落在净输很多的人身上。要根治得把
别的群的比赛记录整份拉到本机，而「本机只装一个群」是这个 App 的地基。

### 串场：去别的球群打（做了一半，另一半是故意不做的）

**已经能做的**：拿一条球局分享链接就能进别人的群、落进那一场
（`src/lib/invite.ts` 一条链接同时办两件事），打完那些比赛记在那个群里。
回自己群之后，在全国榜上按一次「把这个群的也报上去」，两边合成一个人。

**故意不做的：客场战绩不进自己球群的排行榜。**

本群那张榜回答的是「我们这群人之间谁强」。把在别处、跟别人打的场次
算进来，这个问题就没有答案了 —— 而且群里谁都没法核对那些场次。
更实际的一条：MMR 算爆冷要看对手当时的 MMR，而对手是另一个群的人，
本机根本没有他那份记录。

所以「回流」回流到的是**全国榜**，不是本群榜。这是设计，不是没做完。

**真正还欠的**：客场那几场，回自己群之后在 App 里一条都看不见
（不是丢了，是在那个群里，切过去就有）。要让它们在自己这边也看得到，
得先有一份跨群的「我的比赛」，而那要动「本机只装一个群」这条地基。
等真有人开始常串场再说。

### 一键分享战绩（做了，还能再往前一步）

赛后那张卡能存图、能直接调系统分享面板发进群，卡底下有网址和二维码
（二维码是编译期生成的内联 SVG，见 `design/make-qr.py`）。

**还能做的**：扫码进来的人落在首页，还得自己找今晚那一局。
带上球局的邀请链接就能直接落进那一局 —— 但那样二维码得每场重新生成，
要在浏览器里现画，得掂量一下值不值那个库的体积。

### 球局历史再多就得能搜

历史现在默认只列「我打过的」，切「全部」时我参加过的那几场带「我在」标。
十几个人的群这样够用 —— 我的一个月也就六七场。

**再往上就不够了**：几百场之后，连「我打过的」都翻不动，得能按球馆、
按人搜。`OpenSessions` 那一块已经有一个现成的搜索框可以照抄。
等真觉得翻不动了再做。

### 比赛活动

办一场有赛制的比赛，不是平时的轮转局。

### 金币对赌（真的转账）

原来卡在「对手确认记分」后面。**那道门 2026-09-19 开了**：确认、改正、
仲裁三样都有了，一个「两边都不肯让」的局面现在有人能拍板（作废）。

不等于可以做了 —— 真涉及钱之后，「作废」这一下本身会变成被争的东西：
现在它的代价只是几点 MMR，那时候是真钱。至少还要想清楚两件：
作废一场已经付过钱的赌局怎么退，以及管理员和当事人是同一个人时怎么办
（这个球群里管理员就在场上打球）。

---

## 四、技术债

### ~~动态照片每刷一次都重下一遍~~（2026-09-18 改了）

私有桶没有固定地址，每次显示都签一个临时链接 —— 而**浏览器按完整
网址缓存**，签名在查询串里，所以每次签出来的都是一个新网址：手机上
明明有那张图，还是会再下一遍。头像那边不受影响（公开桶，地址固定），
所以这件事只在朋友圈发生，而朋友圈正好是图最多的一屏。

改法是把签好的链接连同到期时间存下来，没过期就接着用同一个网址，
有效期从 1 小时拉到 6（短不等于安全，短了只是让同一张图一天被重下
好几遍）。退出登录和注销账号时清掉。

真 Chromium 打真 HTTP 服务器数出来的：5 次重开同一屏，
**每次新签名重下 4 次，重用同一个地址重下 0 次**。


### 首屏 JS 还有 257 KB（压缩后）

离线包已经从 2733 KB 降到 617 KB，剩下的大头是 JS：
业务代码 143 KB + React 60 KB + supabase-js 54 KB。

（原来记的是 129 KB。涨的那 14 KB 是隐私政策和服务条款
——两份全文、中英各一套，都编在主包里。真要省，
那两份可以拆成按需加载：一年也没人点开第二次。）

再往下砍要按屏拆包，但**这个 App 的 JS 全都会被预缓存**，
拆包只省第一次打开前那一小段，不省下载量。收益有限，先不动。

哪天又觉得慢，先跑 `npm run build` 看 precache 那一行再决定动谁 ——
上一次凭印象猜，盯着 800 KB 的 JS 看了半天，
而真正胖的是 2.1 MB 没人看的衣服。

### 换装素材还能再小一半

现在 1434 KB（已经缩到「真的会被画出来」的尺寸）。
换成 AVIF 还能再省一半左右，但要多写一段格式探测
（canvas 合成，用不了 `<picture>` 的自动回退），
而且 iOS 16 以下认不了。等真觉得不够快再说。

---

## 五、看过了，决定先不动的

这一节不是待办。是**已经摊开看过、当时决定不做**的事 ——
写下来是为了不让它们三个月后再被当成新发现讨论一遍。

### 素材上的第三方商标（2026-09，决定保持原样）

换装素材上有第三方商标。**范围比早先记的大得多**：逐张看过两套素材
之后 —— 女生那套的上衣和裙子上到处是 YY 形状的标，还有一件像队徽；
男生那套几乎每条短裤上都有个对勾，好几件上衣和鞋也有。挑不出干净的，
等于两套 90 张全部重画。

而且**改提示词治不了**：`design/dressup-prompts-male.md` 里一个品牌名
都没有，底图那段本来就写着「不要 logo」—— 标是出图模型自己加回去的。
真要重做，只能每画一张查一张。

**决定：不重画，保持原样。** 换掉底图会让现有球友的人物长相变成另一个人，
这个代价在只有十几个人的时候不值得付。

风险照写在这儿：自己球群玩问题不大；真开放出去、尤其哪天上架应用商店，
这是个真实的法律风险，到那时候再回来看这一条。

出图的底稿已经备好了，哪天决定重做不用从头想：
`design/dressup-prompts-male.md`（反商标的话加在了底图、每一张变体、
存图前的检查三处）和 `design/dressup-prompts-female.md`（新写的 ——
女生那套当初没留出图指令，没有它连重做都无从做起）。
